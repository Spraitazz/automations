


after have deploy version of files in ~/automations
and created ~/automation_configs/{skelbiu,spires}
(and ssh + autologin set up)
--------------------------------------------------
(linux mint 22.1 xfce -> lightdm for logins, light lock for screen locking)

sudo systemctl enable NetworkManager-wait-online.service
sudo systemctl start NetworkManager-wait-online.service

sudo apt install xvfb

conda env create -f environment.yml

mkdir logs

./setup.sh


#
# If you want your user service to start at boot time regardless of login, or before autologin occurs (e.g., on headless systems or servers), then you would enable lingering via:
#
sudo loginctl enable-linger yourusername
